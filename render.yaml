services:
  - type: web
    name: esp32-audio-monitor
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node server/standalone-server.js
    envVars:
      - key: DASHBOARD_TOKEN
        generateValue: true
      - key: DEVICE_TOKEN
        generateValue: true
